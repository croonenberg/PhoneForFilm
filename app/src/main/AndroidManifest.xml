<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 1. App‑brede permissies en features -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Optioneel: camera/sensor‑rights voor inkomende videobel‑mockups -->
    <!-- <uses-permission android:name="android.permission.CAMERA" /> -->

    <application
        android:name=".PhoneForFilmApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.PhoneForFilm">

        <!-- Hoofd‑entry -->
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Nieuw geregistreerd scherm (patch A1) -->
        <activity
            android:name=".presentation.view.ChatsActivity"
            android:exported="false" />

        <!-- Bestaande schermen, ongewijzigd -->
        <activity android:name=".presentation.view.ChatActivity"
            android:exported="false" />
        <activity android:name=".view.IncomingCallActivity"
            android:exported="false" />

        <!-- Eventuele Hilt / WorkManager / Room initialisers -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge" />
    </application>

    <!-- Compile‑SDK, min‑/target‑versies worden in build.gradle(kts) beheerd -->
</manifest>
