name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - uses: gradle/actions/setup-gradle@v3
    - name: Run lint, Detekt and unit tests
      run: ./gradlew lint detekt testDebugUnitTest
